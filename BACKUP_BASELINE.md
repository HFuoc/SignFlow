# Direction B baseline backup

This repository was initialized from the current SmartGlove Dataset Studio working tree after restoring the user-preferred **Direction B — Samsung Health-inspired** visual baseline.

- Source checkpoint: `.design-cache/checkpoints/direction-b-samsung-health-20260909-0345` in the local working sandbox.
- The source files `DeviceManager.tsx`, `LiveMonitor.tsx`, `main.tsx`, `one-ui/shell.tsx`, `one-ui/shell.css`, and `styles.css` were byte-compared against that checkpoint before this backup.
- `backups/Smart-Gloves-direction-b-baseline.zip` contains the cleaned project source with generated caches, `node_modules`, `.venv`, build output, test screenshots, and `.design-cache` excluded.
- The rejected scroll/collapsed-header experiment is not the baseline.

Use this archive as the recovery snapshot while the repository is being populated and further visual refinement continues on a separate branch.
